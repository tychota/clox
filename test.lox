var count = 0;
var initial = 9999;

var current = initial;

while (current > 1) {
  count = count + 1;

  if (current % 2 == 0) {
    current = current / 2;
  } else {
    current = current *3 + 1;
  }
}

print count;